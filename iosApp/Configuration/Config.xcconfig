TEAM_ID=

PRODUCT_NAME=CalcM
PRODUCT_BUNDLE_IDENTIFIER=com.example.calcm.CalcM$(TEAM_ID)

CURRENT_PROJECT_VERSION=1
MARKETING_VERSION=1.0

FRAMEWORK_SEARCH_PATHS = $(inherited) "$(SRCROOT)/../shared/build/xcode-frameworks/$(CONFIGURATION)/$(SDK_NAME)" "$(SRCROOT)/../shared/build/xcode-frameworks/$(CONFIGURATION)/$(PLATFORM_NAME)"
OTHER_LDFLAGS = $(inherited) -framework Shared
